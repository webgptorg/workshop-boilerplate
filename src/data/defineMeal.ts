import type { Meal } from "@/model/types";

/**
 * Small helper which keeps the catalog files readable.
 * Values in `basket` are estimates per portion for diners 7–10 years.
 */
export function defineMeal(meal: Meal): Meal {
  return meal;
}
